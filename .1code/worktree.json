{
  "setup-worktree": [
    "npm install",
    "cp $ROOT_WORKTREE_PATH/.env .env",
    "cp $ROOT_WORKTREE_PATH/.env.local .env.local",
    "cp $ROOT_WORKTREE_PATH/.env.production .env.production",
    "cp $ROOT_WORKTREE_PATH/mcp-server/.env mcp-server/.env",
    "npx prisma generate"
  ]
}
