import { TreningsplanByggerClient } from "@/components/admin/treningsplan-bygger/builder-client";

export const metadata = { title: "Treningsplan-bygger · CoachHQ" };

export default function TreningsplanByggerPage() {
  return <TreningsplanByggerClient />;
}
